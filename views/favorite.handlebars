{{!-- {{#each favorite as |favorites| }}
<div class="flex font-sans favorite"  data-index='{{favorites.recipeID}}'>

  <div class="flex flex-wrap" >
    
    <h2 class="flex-auto text-lg font-bold text-slate-900" data-id='{{favorites.id}}'>
      <!-- Call helper function with triple brackets to render the emoji span as HTML -->
      {{favorites.title}} 
      <button type="button" onclick="deleteId(event)">Delete Recipe</button>
    </h2>
  </div>

  <div class = "flex-none w-48 relative">
      <a href="/favorite/{{favorites.id}}"><img src="{{favorites.image_url}}" class="absolute inset-0 w-full h-full object-cover"/></a>
  </div>
  
</div>
{{/each}}  --}}
<div class="flex flex-wrap">
{{#each favorite as |favorites| }}

<a href="/favorite/{{favorites.id}}">
<div class="shadow bg-gray-700 py-3 px-3 m-4 rounded" data-index='{{favorites.recipeID}}'>
      <img src="{{favorites.image_url}}" class="w-full h-auto"/>
    <div class="p-4">
        <h2 class="text-white leading-tight"> {{favorites.title}} 
      <button type="button" class="ml-20 rounded-md bg-black  text-white" onclick="deleteId(event)">Delete Recipe</button>
        </h2>
    </div>
</div></a>

{{/each}} 
</div>
<script src="/js/deleteId.js"></script>